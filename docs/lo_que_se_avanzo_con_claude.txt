res = X_train.shape[2]
esc = StandardScaler().fit(X_train.reshape(-1,n_features))
X_train = esc.transform(X_train.reshape(-1,n_features)).reshape(X_train.shape)
X_test = esc.transform(X_test.reshape(-1,n_features)).reshape(X_test.shape)

class LSTM2(nn.Module):
    def __init__(self, n_features, hidden=64, layers=2, dropout=0.2):
        super().__init__()
        self.lstm = nn.LSTM(n_features, hidden, num_layers=layers, batch_first=True, dropout=dropout)
        self.drop = nn.Dropout(dropout)
        self.out = nn.Linear(hidden, 1)
    def forward(self, x):
        _, (h,_) = self.lstm(x)
        return self.out(self.drop(h[-1])).squeeze(-1)

modelo = LSTM2(n_features)
pos_weight = torch.tensor([(y_train==0).sum()/(y_train==1).sum()])
crit = nn.BCEWithLogitsLoss(pos_weight=pos_weight)
opt = torch.optim.Adam(modelo.parameters(), lr=1e-3)
Xt = torch.tensor(X_train, dtype=torch.float32)
yt = torch.tensor(y_train, dtype=torch.float32)
loader = torch.utils.data.DataLoader(torch.utils.data.TensorDataset(Xt,yt), batch_size=256, shuffle=True)

t0=time.time()
modelo.train()
for epoca in range(20):
    for xb,yb in loader:
        opt.zero_grad()
        loss = crit(modelo(xb), yb)
        loss.backward()
        opt.step()
print('tiempo entrenamiento:', time.time()-t0)

modelo.eval()
with torch.no_grad():
    proba = torch.sigmoid(modelo(torch.tensor(X_test,dtype=torch.float32))).numpy()
pred = (proba&gt;=0.5).astype(int)
print('F1:', f1_score(y_test,pred), 'AUC:', roc_auc_score(y_test,proba), 'Recall:', recall_score(y_test,pred))
"" completed (exit code 0)

  Read 1 file

Confirmado: el modelo más grande (2 capas, 20 épocas, 397s) da prácticamente el mismo resultado (F1=0.632 vs 0.626) — el modelo simple ya estaba bien convergido, más capacidad no ayuda. Me quedo con la versión simple (más rápida). Documento esto y comiteo.

  Read 1 file

● Update(ml\training\lstm_comparacion.py)
Added 8 lines
un resultado esperado y reportable en la literatura (los modelos de árboles suelen ganarle a las
redes neuronales en datos tabulares con relativamente pocas filas por serie).

Resultado real: LSTM (1 capa, 32 unidades, 8 épocas) da F1=0.626, AUC-ROC=0.723, recall=0.740 —
cumple la meta de recall pero no F1 ni AUC-ROC, y XGBoost lo supera en las 3 métricas
(F1=0.751, AUC-ROC=0.860, recall=0.845). Se probó también una versión más grande (2 capas, 64
unidades, 20 épocas, dropout) para descartar que el modelo simple estuviera subentrenado: dio
prácticamente el mismo resultado (F1=0.632, AUC=0.717) tras 11 veces más tiempo de entrenamiento
(397s vs 36s) — confirma que el modelo simple ya estaba bien convergido, no es un caso de
subentrenamiento, y se mantiene la versión simple por ser igual de buena y mucho más rápida.

Uso:
    python lstm_comparacion.py
"""

Ahora agrego torch a requirements.txt y comiteo.

  Read 1 file

● Update(requirements.txt)
Added 1 line
great-expectations
scikit-learn
xgboost
torch
shap
mlflow
azure-storage-blob

  Committed 7894cfe, pushed to main, ran 1 shell command

Con esto, el componente de ML supervisado queda completo:

┌────────────────────────────────┬─────────┬─────────┬─────────┐
│             Modelo             │   F1    │ AUC-ROC │ Recall  │
├────────────────────────────────┼─────────┼─────────┼─────────┤
│ Regresión Logística (baseline) │ 0.560   │ 0.650   │ 0.603   │
├────────────────────────────────┼─────────┼─────────┼─────────┤
│ Random Forest                  │ 0.773 ✓ │ 0.882 ✓ │ 0.785 ✓ │
├────────────────────────────────┼─────────┼─────────┼─────────┤
│ XGBoost                        │ 0.751 ✓ │ 0.860 ✓ │ 0.845 ✓ │
├────────────────────────────────┼─────────┼─────────┼─────────┤
│ LSTM (Deep Learning)           │ 0.626   │ 0.723   │ 0.740 ✓ │
└────────────────────────────────┴─────────┴─────────┴─────────┘

XGBoost/Random Forest superan al LSTM, tal como el informe anticipaba como resultado válido. Sumado a K-Means e Isolation Forest, todo el componente de ML está cerrado (forecasting removido).

Queda pendiente SHAP (explicabilidad, sección 10.3 del informe) — mencionado en el diseño pero sin implementar todavía. ¿Seguimos con eso, o prefieres pasar a otra cosa (Great Expectations, Terraform, dashboards)?

✻ Cogitated for 54s

❯ ya implementa shap

Implemento SHAP sobre el modelo principal (XGBoost). Según la estructura que documentamos, esto va en ml/evaluation/, no en training/.

  Ran 1 shell command

● Write(ml\evaluation\shap_explicabilidad.py)
Wrote 151 lines to ml\evaluation\shap_explicabilidad.py
"""Explicabilidad del modelo de clasificación principal — SHAP (sección 10.3 del informe).

"Se incorporan valores SHAP sobre el modelo de clasificación principal para explicar, por región
y por predicción individual, qué variables explican en mayor medida el nivel de riesgo estimado
[...] Esto permite que el resultado del modelo sea interpretable para un usuario no técnico, y no
una 'caja negra'."

Reentrena el mismo XGBoost de ml/training/random_forest_xgboost.py (mismos datos, mismas
features, mismos hiperparámetros) y calcula SHAP sobre el set de test, con 3 niveles de detalle:
1. Importancia global (promedio de |SHAP| por feature) — qué variables explican el riesgo en
… +141 lines

Ahora lo pruebo con los datos reales.

  Ran 1 shell command

Verifico los casos individuales explicados (para confirmar que sean interpretables de verdad).

  Read 1 file, ran 1 shell command

Excelente, ambos casos son genuinamente interpretables: el acierto (Apurímac, oct 2022, 99.8% de probabilidad) se explica por alta tasa histórica + racha reciente + pico inusual de focos de calor; el error (Moquegua, sep 2021, solo 2.7% predicho pero sí hubo emergencia) se explica honestamente por baja tasa histórica y cero señales de alerta previas — exactamente el tipo de "sorpresa" que es difícil de anticipar. Comiteo.

  Committed d94c9f3, pushed to main

El componente completo de Machine Learning (sección 10 del informe) queda cerrado:
- 10.1 Supervisado: Regresión Logística, Random Forest/XGBoost, LSTM ✓
- 10.2 No supervisado: K-Means, Isolation Forest ✓
- 10.3 Explicabilidad: SHAP ✓

Queda pendiente 10.4 Chatbot (RAG con Azure OpenAI), y aparte: Terraform (infra/), Great Expectations (data_quality/), y los dashboards de Power BI. ¿Con cuál seguimos?

✻ Baked for 1m 33s

❯ quality y pipelines?

Son dos casos distintos — uno lo puedo probar de verdad ahora mismo, el otro no (depende de tener Azure desplegado). Empiezo por Great Expectations (data/quality/), reintentando la instalación que había fallado antes.

  Ran 2 shell commands

Confirmado el problema: Great Expectations requiere numpy<2.0, que no tiene wheels precompilados para Python 3.14 (muy nuevo) — necesitaría compilarlo desde código fuente, y el compilador local es muy viejo para eso. No es algo que deba forzar tocando el compilador del sistema. Reviso una alternativa más liviana (pandera) antes de decidir qué hacer.

  Ran 1 shell command

Pandera se instaló bien (compatible con Python 3.14). Antes de seguir, una decisión rápida:

● User answered Claude's questions:
· Great Expectations no se puede instalar en este entorno (Python 3.14 muy nuevo). ¿Cómo manejamos data/quality/? → Usar pandera como reemplazo (Recomendado)

● Write(data\quality\schemas.py)
Wrote 89 lines to data\quality\schemas.py
"""Esquemas declarativos de calidad de datos (Bronze -> Silver), con pandera.

El informe (sección 4.4) especifica Great Expectations para esta validación, pero esa librería
requiere numpy < 2.0, que no tiene wheels precompilados para Python 3.14 (muy reciente) y falla
al compilar desde código fuente en este entorno (requiere GCC >= 8.4, no disponible). Se usa
pandera en su lugar: misma idea (esquema declarativo de tipos/rangos/nulabilidad como única
fuente de verdad, en vez de validaciones sueltas repetidas en cada script), compatible con
Python 3.14 y numpy 2.x.

Cada esquema formaliza las reglas ya usadas en los reportes de calidad de cada script de Silver
… +79 lines

Ahora el script que corre estos esquemas contra los datos reales de Silver.

● Write(data\quality\validar_silver.py)
Wrote 88 lines to data\quality\validar_silver.py
"""Valida los datasets de Silver contra los esquemas declarativos de schemas.py.

Corresponde a la validación de calidad Bronze -> Silver de la sección 4.4 del informe (ver
schemas.py para el porqué de pandera en vez de Great Expectations). Se corre después de cada
script de Silver (data/silver/*/limpieza_*.py), sobre su resultado ya escrito en parquet.

Uso:
    python validar_silver.py
"""
… +78 lines

Ahora lo pruebo contra los datos reales de Silver.

  Ran 1 shell command

Todos pasan — pero verifico que el esquema realmente detecte errores (no que esté pasando todo por ser demasiado laxo), inyectando un valor inválido de prueba.

  Ran 1 shell command

Confirmado — el esquema detecta correctamente los 3 errores inyectados (Ubigeo inválido, región inexistente, valor negativo). Comiteo esto.

  Read 1 file, ran 1 shell command

❯ en produccion que modelos de ml se usaran? tanto pa lo demas commo pa el poewr bi

● Update(requirements.txt)
Added 1 line, removed 1 line
python-dotenv
pandas
geopandas
great-expectations
pandera
scikit-learn
xgboost
torch

  Committed 1740093, pushed to main

En producción, solo 4 componentes de ML pasan a servir resultados reales — los demás fueron comparaciones/descartes que ya cumplieron su propósito:

Van a producción:
1. XGBoost (o Random Forest, cumplen las 3 metas) — el modelo de clasificación de riesgo. Genera probabilidad_riesgo en FACT_PREDICCIONES.
2. K-Means — genera cluster_riesgo en DIM_REGION.
3. Isolation Forest — marca días atípicos en FACT_MONITOREO_DIARIO (señal independiente de alerta).
4. SHAP — no es un modelo de predicción, es la capa de explicabilidad que corre junto al XGBoost para poder decir "por qué" de cada predicción.

No van a producción (cumplieron su rol de comparación, se documentan pero no se despliegan):
- Regresión Logística — solo era el baseline de comparación.
- LSTM — perdió contra XGBoost en las 3 métricas.
- Prophet/SARIMA — ya lo quitamos del proyecto completo.

Sobre Power BI específicamente — algo importante que aclarar: Power BI no ejecuta ningún modelo. Los modelos corren en Azure Databricks (orquestados por Azure Data Factory, versionados en Azure ML/MLflow), y escriben sus resultados ya calculados directamente a las tablas de Gold (FACT_PREDICCIONES, DIM_REGION.cluster_riesgo). Power BI solo lee esas tablas ya pobladas vía Azure Synapse Serverless SQL Pool — es una capa de consumo, no de cómputo. El dashboard 9.3 (Riesgo Dinámico) y el chatbot funcionan así: consultan resultados pre-calculados, no corren XGBoost en vivo cada vez que alguien abre el dashboard.

✻ Crunched for 3m 23s

❯ que falta ahora?

Buen momento para hacer un balance completo. Esto es lo que queda del diseño original del informe:

Ya completo y validado con datos reales:
- ✅ Las 4 fuentes + INEI + ONI (Bronze → Silver → Gold)
- ✅ Modelo dimensional completo (3 dimensiones + 2 hechos)
- ✅ ML: clasificación (supera las 3 metas), clustering, detección de anomalías, SHAP
- ✅ Calidad de datos (pandera)

Falta construir:
1. Terraform (infra/) — está vacío, solo el README. Define los recursos de Azure (ADLS, Databricks, Synapse, etc.) pero nada se ha desplegado todavía.
2. Dashboards de Power BI (apps/dashboards/) — están diseñados (los 5, sección 9) pero no construidos. Esto sí se puede empezar ya mismo contra los archivos Gold locales, sin esperar a Azure.
3. Chatbot RAG (apps/chatbot/) — necesita Azure OpenAI + AI Search, no se puede probar sin Azure desplegado.
4. App web (apps/webapp/) — depende de que los dos anteriores existan primero.
5. CI/CD (.github/workflows/) — se puede escribir el YAML ya, aunque no se pueda "correr" de verdad sin Azure.
6. MLflow / registro de modelos — necesita Databricks/Azure ML.

Lo más lógico por orden de dependencia: Power BI ahora (no depende de Azure, ya tenemos los datos), y Terraform en paralelo o después (para cuando quieras desplegar de verdad). El chatbot y la webapp quedan bloqueados hasta tener Azure.

¿Con cuál seguimos?

✻ Crunched for 35s

❯ donde puedo ver el diagrama de mis tablas?

Ahora mismo el único diagrama está en el informe (ASCII art en la sección 5.3, ya desactualizado en formato aunque el contenido lo fui actualizando). Te armo uno visual real y navegable como artifact, reflejando el esquema actual exacto (con los campos granulares de severidad, oni, etc.).

● Skill(artifact-design)
Successfully loaded skill

  Ran 1 shell command

❯ puedes colocar ya el ci cd en worflows github

❯ dame en png el diagrama

Voy con las dos cosas. Primero el PNG del diagrama — reviso si tengo herramientas para generarlo de verdad.

  Ran 1 shell command

Node/npm disponibles. Instalo mermaid-cli para generar el PNG real.

  Ran 1 shell command

Instalado. Ahora escribo el diagrama con el esquema real y lo renderizo a PNG.

  Made 1 scratchpad edit +57, read 1 file, listed 1 directory, ran 1 shell command

El diagrama se ve bien. Lo copio a docs/ de tu proyecto para que quede accesible localmente.

  Ran 1 shell command

El diagrama está en docs/modelo_gold.png (no se sube a git, ya que docs/ está excluido, pero queda en tu carpeta local). Ahora construyo los workflows de CI/CD en .github/workflows/.

  Read 1 file

Antes de escribir los workflows, verifico algo importante: infra/ está vacío (sin Terraform aún) y los datos de Silver no están en git (por diseño). Voy a construir lo que sí puede correr de verdad hoy (lint/sintaxis de Python), y dejar los pasos que necesitan Azure claramente marcados como pendientes de secretos. Primero valido localmente que todo el código compila sin errores.

  Ran 6 shell commands
Read ..\..\..\AppData\Local\Temp\claude\C--Users-yeshu-Documents-Inteligencia-de-Negocios-Proyecto\4f930303-f61e-4213-9559-888ebb5cda48\scratchpad\modelo_gold.mmd (57 lines)
Read requirements.txt (13 lines)
Read data\quality\validar_silver.py (89 lines)
Read data\quality\schemas.py (90 lines)
Read ..\..\..\AppData\Local\Temp\claude\docx_extract\shap_casos.txt (78 lines)
Skills restored (artifact-design)

Good, DateTime works. Now fixing the three real lint issues in schemas.py, limpieza_indeci.py, and kmeans_clustering.py.

● Update(data\quality\schemas.py)
Added 1 line, removed 1 line
from pathlib import Path

import pandera.pandas as pa
from pandera.typing import Series
from pandera.typing import DateTime, Series

sys.path.insert(0, str(Path(__file__).parent.parent / "ingestion" / "open_meteo"))
from regiones_peru import REGIONES_CAPITALES  # noqa: E402

  Ran 1 shell command

Now the re import in limpieza_indeci.py and the unused mejor_modelo in kmeans_clustering.py.

  Read 1 file, ran 1 shell command

Confirmed genuinely unused — removing it.

● Update(data\silver\indeci\limpieza_indeci.py)
Removed 1 line
from __future__ import annotations

import json
import re
import zipfile
from datetime import datetime, timezone
from pathlib import Path

  Read 1 file, ran 1 shell command

mejor_modelo is tracked but never used after selection — safe to drop, just keep the labels/score/k.

● Update(ml\training\kmeans_clustering.py)
Added 2 lines, removed 2 lines
    X_esc = StandardScaler().fit_transform(X)

    resultados_k = {}
    mejor_k, mejor_score, mejor_modelo, mejor_labels = None, -1, None, None
    mejor_k, mejor_score, mejor_labels = None, -1, None
    for k in K_CANDIDATOS:
        modelo = KMeans(n_clusters=k, random_state=RANDOM_STATE, n_init=10)
        labels = modelo.fit_predict(X_esc)
        score = silhouette_score(X_esc, labels)
        resultados_k[k] = round(score, 4)
        if score > mejor_score:
            mejor_k, mejor_score, mejor_modelo, mejor_labels = k, score, modelo, labels
            mejor_k, mejor_score, mejor_labels = k, score, labels

    df = df.copy()
    df["cluster_riesgo"] = mejor_labels

Now let's re-run the full lint check to confirm everything is clean, and re-verify syntax compiles.

  Ran 7 shell commands

Now writing the CI workflow with the stages that can genuinely run today, and a separate one gated on Terraform files existing.

  Ran 1 shell command

● Write(.github\workflows\ci.yml)
                                                                                                                        ~609k uncached · /clear to start fresh
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents · shift+click to native select


stop: configúralo a los 10 minutos de inactividad (para que no quede corriendo sin usar).
7. Create.

Dime qué opciones de "Type" te aparecen exactamente (a veces depende de la región si Serverless está disponible) y seguimos.

✻ Sautéed for 33s

※ recap: Estás armando la infraestructura de Azure para tu proyecto BI a mano (ya tienes Storage, Key Vault, ADF y Databricks funcionando). Ahora toca crear un SQL Warehouse Serverless en Databricks para que Power BI pueda consultar tus datos, en vez de Synapse (que falló por restricciones de la cuenta nueva).

❯ Microsoft Azure
  Search data, notebooks, recents, and more...
  CTRL + P
  SQL Warehouses
  sqlwh-cenepred-dev.
  Overview
  Connection details
  Monitoring
  Status
  Running
  Name
  sqlwh-cenepred-dev. (ID: cabc343c2bbc4249)
  Type
  Serverless
  Cluster size
  2X-Small
  Auto stop
  After 10 minutes of inactivity
  Scaling
  Cluster count: Active 1 Min 1 Max 1
  Channel
  Current
  (v 2026.20)
  Created by
  yeshuachavezlozano@gmail.com
  Manage permissions
  Do you want information about permission levels? Learn more
  Type to add multiple users or groups
  yeshuachavezlozano@gmail.com
  Admins

  Can manage

Excelente, el SQL Warehouse está Running, tipo Serverless, sin ningún problema de cuota — esto reemplaza perfectamente el rol que iba a cumplir Synapse.

Un paso útil ahora: ve a la pestaña "Connection details" (arriba, junto a Overview) y anota el Server hostname y el HTTP path — son los datos que Power BI va a necesitar más adelante para conectarse a este warehouse y consultar tu capa Gold.

Con esto, tu roadmap de infraestructura queda así:
- ✅ Resource Group, Storage (Data Lake), Key Vault, Data Factory
- ✅ Databricks (cluster + SQL Warehouse Serverless, reemplazando a Synapse)
- ⏳ Power BI (conectar al SQL Warehouse)
- ⏳ Chatbot (Azure OpenAI + AI Search)
- ⏳ Webapp

¿Seguimos con Power BI ahora, o prefieres pausar la parte de infraestructura por hoy?

✻ Cogitated for 55s

❯ Microsoft Azure
  Search data, notebooks, recents, and more...
  CTRL + P
  SQL Warehouses
  sqlwh-cenepred-dev.
  Overview
  Connection details
  Monitoring
  Use these details to connect to this warehouse

  Create a personal access token
  Server hostname
  adb-7405616359428930.10.azuredatabricks.net
  Workspace ID
  7405616359428930
  HTTP path
  /sql/1.0/warehouses/cabc343c2bbc4249
  JDBC URL
  jdbc:databricks://adb-7405616359428930.10.azuredatabricks.net:443/default;transportMode=http;ssl=1;AuthMech=3;httpPath=/sql/1.0/warehouses/cabc343c2bbc4249;
  Databricks supports drivers released within the last two years. Download drivers here
  OAuth URL
  https://adb-7405616359428930.10.azuredatabricks.net/oidc


Buen dato, guarda esos tres valores (Server hostname, HTTP path) — los vas a necesitar en Power BI.

Antes de eso, necesitas un token de acceso para autenticarte desde Power BI (Databricks no te deja conectar solo con usuario/contraseña desde afuera):

1. Haz clic en "Create a personal access token" (el link que ves arriba de "Server hostname").
2. Dale un nombre descriptivo, ej. powerbi-connection, y una fecha de expiración (ej. 90 días, para no dejarlo indefinido).
3. Copia el token apenas se genere — Databricks solo te lo muestra una vez, si cierras esa ventana sin copiarlo hay que crear uno nuevo.
4. Guárdalo en un lugar seguro (no lo pegues aquí en el chat, igual que la key del storage).

¿Ya tienes Power BI Desktop instalado en tu computadora, o hay que instalarlo primero?

✻ Sautéed for 1m 1s

❯ Microsoft Azure
  Search data, notebooks, recents, and more...
  CTRL + P
  Settings
  Workspace admin
  Appearance
  Identity and access
  Security
  Compute
  Development
  Notifications
  Advanced
  User
  Profile
  Preferences
  Developer
  Linked accounts
  Notifications
  User settings
  Developer
  Access tokens
  Personal access tokens can be used for secure authentication to the Databricks API.

  No tokens exist.
  Generate new token
  Name
  powerbi-connection
  Lifetime (days)
  90
  Scope

  BI Tools

  Other APIs
  API scope(s)
  Scopes restrict which API endpoints this token can access. Learn more
  sql


Esa configuración está bien — el scope "sql" (o "BI Tools", que probablemente ya lo incluye) es justo lo que necesitas para que Power BI consulte el warehouse. Dale a "Generate new token" (o el botón de confirmar) y copia el token que te muestre — recuerda que solo se ve una vez.

✻ Crunched for 27s

❯ ya lo copie

Perfecto. Ahora, ¿ya tienes Power BI Desktop instalado en tu computadora? Es gratis, se descarga desde la Microsoft Store o desde powerbi.microsoft.com/desktop. Si no lo tienes, instálalo primero y avísame cuando esté listo para configurar la conexión al SQL Warehouse.

✻ Sautéed for 16s

❯ ya esta instalado

Bien, conectemos Power BI Desktop al SQL Warehouse.

1. Abre Power BI Desktop → "Home" → "Get Data" → busca "Azure Databricks" en la lista de conectores → Connect.
2. Server Hostname: pega el que copiaste (adb-7405616359428930.10.azuredatabricks.net).
3. HTTP Path: pega el que copiaste (/sql/1.0/warehouses/cabc343c2bbc4249).
4. Data Connectivity mode: elige "Import" por ahora (más simple; DirectQuery lo vemos después si hace falta).
5. En la ventana de autenticación, elige "Personal Access Token" y pega el token que generaste.
6. Connect.

Un aviso importante antes de que sigas: esto va a conectar bien, pero todavía no vas a ver tus tablas Gold ahí — tu capa Gold (dim_tiempo, fact_emergencias, etc.) solo existe como archivos Parquet en tu computadora, nunca se subió a Databricks/Unity Catalog. Primero verifiquemos que la conexión técnica funcione (verás el catálogo vacío o con ejemplos), y después vemos cómo subir tus datos Gold reales para que Power BI tenga algo que mostrar.

Avísame si la conexión conecta bien.

✻ Crunched for 38s

※ recap: Estás armando la infraestructura Azure del proyecto BI a mano para aprender; Databricks y el SQL Warehouse ya funcionan. Siguiente paso: confirma si Power BI Desktop logró conectarse al warehouse con el token que pegaste.

❯ ya connect y ya estoy viendo 3 carpetas dbw samples y system

Confirmado, la conexión funciona — esas 3 carpetas (dbw, samples, system) son catálogos de ejemplo/sistema de Databricks, no tus datos todavía.

Ahora subamos tu capa Gold real. Dos pasos:

1. Sube tus archivos Parquet de Gold al Storage Account
1. Ve al portal de Azure → stcenepreddev1 → "Storage browser" → container gold.
2. Click en "Upload" → selecciona los 5 archivos de tu carpeta local data/gold/local_data/*.parquet (dim_tiempo, dim_region, dim_fenomeno, fact_emergencias, fact_monitoreo_diario — y si ya lo tienes, fact_gasto_prevaed).
3. Sube todos.

2. Regístralos como tablas en Databricks (Unity Catalog)
En un notebook nuevo de Databricks, corre algo así por cada tabla (ejemplo con dim_tiempo):
df = spark.read.parquet("abfss://gold@stcenepreddev1.dfs.core.windows.net/dim_tiempo.parquet")
df.write.format("delta").mode("overwrite").saveAsTable("dbw.default.dim_tiempo")

Empieza subiendo los archivos y avísame cuando estén en el container gold.

✻ Worked for 48s

❯ "fact_monitoreo_diario.parquet" "dim_fenomeno.parquet" "dim_region.parquet" "dim_tiempo.parquet" "fact_emergencias.parquet" "fact_gasto_prevaed.parquet"  estos todos'

Sí, esos son los 6 archivos correctos (3 dimensiones + 3 hechos, incluyendo el de MEF que armamos). Súbelos todos al container gold si no lo has hecho ya, y luego en un notebook de Databricks corre esto para registrar las 6 como tablas:

tablas = [
    "dim_tiempo",
    "dim_region",
    "dim_fenomeno",
    "fact_emergencias",
    "fact_monitoreo_diario",
    "fact_gasto_prevaed",
]

for tabla in tablas:
    df = spark.read.parquet(f"abfss://gold@stcenepreddev1.dfs.core.windows.net/{tabla}.parquet")
    df.write.format("delta").mode("overwrite").saveAsTable(f"dbw.default.{tabla}")
    print(f"OK: {tabla} ({df.count()} filas)")

Corre esa celda y pega aquí el resultado (debería imprimir las 6 líneas con "OK" y el conteo de filas de cada una).

✻ Worked for 9s

※ recap: Building out Azure infrastructure for your BI project. Databricks and its SQL Warehouse are live and connected to Power BI; now run the notebook cell that registers your 6 Gold parquet files as Delta tables, then share the row-count output.

❯ y4JJavaError: An error occurred while calling o523.parquet.
  : Failure to initialize configuration for storage account stcenepreddev1.dfs.core.windows.net: Invalid configuration value detected for fs.azure.account.keyInvalid configuration value detected for fs.azure.account.key
      at shaded.databricks.azurebfs.org.apache.hadoop.fs.azurebfs.services.SimpleKeyProvider.getStorageAccountKey(SimpleKeyProvider.java:52)

Ese error es porque este es un notebook nuevo, y la configuración de la key de storage que hiciste antes fue solo para el notebook anterior (no se comparte automáticamente entre notebooks). Agrega esta celda primero, ANTES del loop de las tablas, con tu key real:

spark.conf.set(
    "fs.azure.account.key.stcenepreddev1.dfs.core.windows.net",
    "TU_ACCESS_KEY_AQUI"
)

Córrela, y después corre el loop de las 6 tablas de nuevo. Si quieres evitar tener que repetir esto en cada notebook nuevo, después te muestro cómo ponerlo directo en la configuración del cluster (así queda fijo para todos los notebooks que uses con ese cluster). Por ahora, prueba con esto y dime si el loop corre bien.

✻ Worked for 13s

❯
  [SCHEMA_NOT_FOUND] The schema `dbw.default` cannot be found. Verify the spelling and correctness of the schema and catalog.
  If you did not qualify the name with a catalog, verify the current_schema() output, or qualify the name with the correct catalog.
  To tolerate the error on drop use DROP SCHEMA IF EXISTS. SQLSTATE: 42704

Falta crear el schema antes de escribir las tablas — "dbw" es el catálogo pero "default" no existe adentro todavía. Agrega esta línea antes del loop:

spark.sql("CREATE SCHEMA IF NOT EXISTS dbw.default")

Córrela, y después corre el loop de las 6 tablas otra vez.

✻ Worked for 15s

❯ [NO_SUCH_CATALOG_EXCEPTION] Catalog 'dbw' was not found. Please verify the catalog name and then retry the query or command again. SQLSTATE: 42704

Mi suposición sobre el nombre "dbw" estaba mal. Vamos a ver los catálogos reales que existen. En una celda, corre:

spark.sql("SHOW CATALOGS").show(truncate=False)

Pega aquí el resultado — con el nombre real del catálogo, ajustamos el resto del código (CREATE SCHEMA y el loop de tablas) para usar ese nombre en vez de "dbw".

✻ Worked for 10s

❯ just now (2s)
  4
  4
  spark.sql("SHOW CATALOGS").show(truncate=False)
  +----------------+
  |catalog         |
  +----------------+
  |dbw_cenepred_dev|
  |samples         |
  |system          |
  +----------------+

Ahí está — el catálogo real es dbw_cenepred_dev (con guion bajo, no "dbw" solo). Corrige así:

spark.sql("CREATE SCHEMA IF NOT EXISTS dbw_cenepred_dev.default")

tablas = [
    "dim_tiempo",
    "dim_region",
    "dim_fenomeno",
    "fact_emergencias",
    "fact_monitoreo_diario",
    "fact_gasto_prevaed",
]

for tabla in tablas:
    df = spark.read.parquet(f"abfss://gold@stcenepreddev1.dfs.core.windows.net/{tabla}.parquet")
    df.write.format("delta").mode("overwrite").saveAsTable(f"dbw_cenepred_dev.default.{tabla}")
    print(f"OK: {tabla} ({df.count()} filas)")

Corre esto y pega el resultado.

✻ Worked for 12s

❯ OK: dim_tiempo (4383 filas)
  OK: dim_region (25 filas)
  OK: dim_fenomeno (72 filas)
  OK: fact_emergencias (84369 filas)
  OK: fact_monitoreo_diario (109575 filas)
  OK: fact_gasto_prevaed (300 filas)

Todas las 6 tablas cargadas correctamente, y los conteos coinciden exactamente con lo que ya conocías (4383 días, 25 regiones, 300 filas de MEF = 25×12). Tu capa Gold ya está real en la nube.

Ahora vuelve a Power BI Desktop:
1. En el panel de navegación (Navigator) donde ya tenías la conexión abierta, dale "Refresh" (o si ya cerraste esa ventana, entra de nuevo por "Get Data" → tu conexión reciente a Databricks).
2. Busca el catálogo dbw_cenepred_dev → schema default → deberías ver las 6 tablas listadas.
3. Marca las 6 (los checkboxes) → "Load".

Avísame cuando termine de cargar — con eso ya tendrías tu primer dashboard armable de verdad, con datos reales desde Azure.

✻ Baked for 11s

❯ ya cargó, cómo armo el dashboard

Antes de cualquier gráfico, hay que decirle a Power BI cómo se conectan tus tablas — eso se hace en la vista de Modelo.

1. Configura las relaciones
1. En Power BI Desktop, ve a la vista "Model" (ícono de tablas conectadas, en el panel izquierdo vertical).
2. Power BI a veces detecta relaciones solo, pero verifica/crea estas manualmente (arrastra de un campo al otro si no existen):
  - DIM_TIEMPO[fecha_id] → FACT_EMERGENCIAS[fecha_id]
  - DIM_REGION[region_id] → FACT_EMERGENCIAS[region_id]
  - DIM_FENOMENO[fenomeno_id] → FACT_EMERGENCIAS[fenomeno_id]
  - DIM_TIEMPO[fecha_id] → FACT_MONITOREO_DIARIO[fecha_id]
  - DIM_REGION[region_id] → FACT_MONITOREO_DIARIO[region_id]
  - DIM_REGION[region_id] → FACT_GASTO_PREVAED[region_id]
3. FACT_GASTO_PREVAED NO la conectes a DIM_TIEMPO (recuerda: es anual, no diaria — se queda suelta, se filtra por su propia columna anio directamente).

2. Elige con cuál dashboard empezar

Tienes 5 planeados en tu informe (Monitoreo Diario, Histórico y Tendencias, Riesgo Dinámico, Comparativo Regional, Impacto Socioeconómico). Te recomiendo empezar por Comparativo Regional — es el más simple de armar primero (un mapa + un ranking de barras por región) y te da una victoria rápida para agarrar confianza con la herramienta.

¿Armamos las relaciones primero y me confirmas cuando estén, o prefieres que te guíe directo con el primer gráfico?

✻ Cogitated for 25s

❯ entiendo, ya estan conectadas las tablas, ahora la parte del azure ya esta? ya esta automatizada? no entiendo que hicimos hasta ahora

Buena pregunta para parar y aclarar. Aquí el estado real, sin rodeos:

Lo que SÍ existe en Azure ahora:
- Resource Group, Storage Account (Data Lake con bronze/silver/gold), Key Vault, Data Factory — todos creados.
- Databricks con un cluster real y un SQL Warehouse, conectados a tu storage.
- Tus 6 tablas Gold reales, cargadas en Databricks (Unity Catalog), y Power BI ya las está leyendo desde ahí.

Lo que NO está automatizado — hoy hicimos todo a mano:
- Subiste los 6 archivos Parquet al container gold manualmente (drag and drop desde tu compu).
- Corriste el notebook de Databricks manualmente (tú le diste "run" a las celdas).
- Tus scripts reales (limpieza_indeci.py, dim_tiempo.py, etc.) siguen corriendo en tu computadora local, no en Azure. Lo único que subimos fue el resultado final (los Parquet ya procesados).
- Data Factory está vacío — lo creamos pero nunca le configuramos ningún pipeline. Hoy no orquestó nada.

En resumen: hicimos una prueba de concepto manual para confirmar que cada pieza de Azure funciona y se conecta entre sí — no un pipeline automático de producción. Si mañana cambias tus datos localmente, tendrías que repetir todo el proceso a mano otra vez (correr scripts local → subir Parquets → correr notebook).

¿Quieres que sigamos armando los dashboards de Power BI con lo que ya tienes (dejando la automatización para después), o prefieres que ahora sí conectemos Data Factory para que esto se actualice solo?

✻ Baked for 22s

❯ tmb falta poner contenido a bronze y silver

Buen ojo. Démosle esa misma pasada manual que le dimos a Gold — subir lo que ya tienes local a los containers bronze y silver, para que la estructura completa del Medallion exista en la nube (aunque siga siendo manual por ahora, igual que Gold).

Bronze: sube cada subcarpeta de fuente tal cual la tienes localmente en data/bronze/{fuente}/local_data/ — manteniendo la misma organización:
1. En el Storage Browser del portal, entra al container bronze.
2. Crea una carpeta por cada fuente (indeci, open_meteo, usgs, nasa_firms, noaa_oni, inei_limites, mef_pp0068) y sube ahí los archivos crudos correspondientes (zips, csv, json, xls) de tu data/bronze/{fuente}/local_data/.

Silver: mismo proceso, container silver, una carpeta por fuente, subiendo los .parquet de data/silver/{fuente}/local_data/.

Esto va a pesar más que Gold (especialmente NASA FIRMS, que tenía ~1.5M de filas) — puede tardar un rato en subir. ¿Empezamos por Bronze o prefieres primero terminar de armar el dashboard de Power BI y dejamos esto para después?

✻ Cogitated for 28s

❯ eso ya lo hariamos despues no? ahora por el momento powerbi ta bien?

Sí, tiene sentido dejarlo para después — no bloquea nada de Power BI, ya tienes Gold que es lo único que Power BI necesita consultar. Bronze/silver en la nube es más por completar la arquitectura y para cuando automatices con Data Factory, no urgente ahora.

Sigamos con el dashboard entonces. Como quedamos, empecemos con Comparativo Regional:

1. En el panel de la derecha (Fields/Campos), busca DIM_REGION.
2. En el lienzo del reporte, inserta un visual de mapa (ícono de mapa en el panel de Visualizations) — arrastra DIM_REGION[departamento] al campo "Location" del visual.
3. Arrastra FACT_EMERGENCIAS[cantidad_afectados] (o cualquier medida de daño) al campo "Size" — esto hace que cada región se vea más grande/chica según el total de afectados.
4. Al lado, agrega un gráfico de barras horizontales: eje Y = DIM_REGION[departamento], eje X = suma de FACT_EMERGENCIAS[cantidad_afectados], ordenado de mayor a menor.

Prueba estos dos primeros visuales y cuéntame qué tal se ven con tus datos reales.

✻ Crunched for 23s

※ recap: Estás construyendo el proyecto BI de riesgo climático en Azure, y ya tienes Databricks y las tablas Gold conectadas a Power BI. Ahora toca armar el dashboard Comparativo Regional: agrega un mapa y un gráfico de barras usando región y cantidad de afectados.

❯ hay un problema en el mapa le clasifique como ciudades, y hay algunas q estan en peru pero otras en otros paises como soluciono eso

Eso es porque el mapa está usando geocodificación por nombre (Bing busca "Piura" o "Ica" en el mundo entero y a veces encuentra una ciudad homónima en otro país). La solución correcta: tu DIM_REGION ya tiene columnas reales de latitud y longitud (las coordenadas de la capital de cada región que armamos en Gold) — úsalas directo en vez de dejar que adivine por nombre.

1. En el visual del mapa, quita el campo departamento del pozo "Location".
2. Arrastra DIM_REGION[latitud] al pozo "Latitude".
3. Arrastra DIM_REGION[longitud] al pozo "Longitude".
4. Puedes dejar departamento como campo de "Legend" o "Tooltips" si quieres que se identifique cada punto al pasar el mouse.

Con coordenadas exactas en vez de nombres, no hay ambigüedad posible — cada punto cae exactamente donde le corresponde en Perú.

✻ Churned for 20s

❯ Error subyacente: DataViewMappingError_LatLongAggregateWithNoLocation
  Identificador de actividad: 2353abf5-8d3b-4417-a45e-4f99dee2d4ec                                                                                            Hora: Sun Jul 26 2026 13:58:33 GMT-0500 (hora estándar de Perú)ud y longitud en no resumir dice
                                                                                                                        ~609k uncached · /clear to start fresh
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ accept edits on (shift+tab to cycle) · ← for agents · shift+click to native select